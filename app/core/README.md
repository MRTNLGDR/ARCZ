# Core V2 do navegador

Contém contratos transversais: origem, eventos, callbacks seguros, transação,
capabilities, jobs, budget, inputs, rede e staging. Não importe `ui.js` daqui e
não acesse provider remoto. Módulos de domínio dependem do core; o core não
depende deles.
