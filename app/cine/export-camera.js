import { evaluateTrack } from "./keyframes.js";
export function exportCameraJSON(timeline,{frameStart=0,frameEnd=timeline.duration_frames,step=1,coordinateSystem="local_enu"}={}){const cameraTracks=timeline.tracks.filter(t=>t.target==="camera"||t.target==="lens"||t.target==="focus");const frames=[];for(let frame=frameStart;frame<=frameEnd;frame+=step){const values={};for(const track of cameraTracks)values[`${track.target}.${track.property}`]=evaluateTrack(track,frame);frames.push({frame,time_seconds:frame/timeline.fps,values});}return{schema_version:1,coordinate_system:coordinateSystem,fps:timeline.fps,frame_start:frameStart,frame_end:frameEnd,frames};}
export function downloadCameraJSON(data,name="arcz-camera.json"){const blob=new Blob([JSON.stringify(data,null,2)],{type:"application/json"});const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),0);}
