# Wheelhouse Python local

Coloque aqui um `wheelhouse/` produzido e auditado em máquina autorizada. O
runtime de release deve instalar com `pip --no-index --find-links` e registrar
SHA-256/licença no manifesto de distribuição. Nenhuma instalação remota é feita
pelo ARCZ em `offline_strict`.
